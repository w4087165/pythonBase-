lambda 表达式（又陈匿名函数）
	作用：
		创建一个匿名函数对象
		同def 类似，但不提供函数名
	语法格式：
		lambda [函数的形参列表]：表达式
		注：[]代表里面的内容可省
		myadd = lambda x,y : x+y
		
	示例见：
	说明：
		1.lambda 表达式创建的函数只能包含一条表达式
		2.lambda 比函数简单且可以随时创建和销毁，由于减少程序的耦合度
